#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long

signed main() {
    ios::sync_with_stdio(false); 
    cin.tie(NULL);  

    int tc; cin >> tc;
    while(tc--){
        int y,x; cin >> y >> x;
        // since y,x goes to 1e9 , solution must be of O(n);
        // Not able to solve this, will think again after some time;
                
    }
}
